# Azure Storage Account for Terraform State
# Copy this file to .env and fill in your values
#
# Bootstrap order:
#   1. Fill in values below
#   2. cd modules/remote-state && make bootstrap
#   3. cd environments/development && make init && make plan && make apply

# Storage account name (must be globally unique, 3-24 lowercase alphanumeric)
STORAGE_ACCOUNT_NAME=yourstorageaccount

# Blob container name for state files
CONTAINER_NAME=tfstate

# Resource group where the storage account will be created
STATE_RESOURCE_GROUP=terraform-state-rg

# ACR name (must match registry_name in variables.tf)
ACR_NAME=aksmultiregiondemoacr
