# =============================================================================
# AWS Configuration
# =============================================================================
AWS_DEFAULT_REGION=us-east-1

# Terraform State
AWS_REMOTE_BUCKET_NAME=your-terraform-state-bucket
AWS_REMOTE_DYNAMODB_TABLE=your-terraform-lock-table

# ECR Configuration (populated after registries/aws deployment)
AWS_ECR_REPO_NAME=basic-demo-microservice-01
AWS_ECR_REPO_ARN=arn:aws:ecr:us-east-1:ACCOUNT_ID:repository/basic-demo-microservice-01
AWS_ACCOUNT_ID=your-aws-account-id

# EKS Cluster Names
AWS_CLUSTER_EAST=eks-cluster-dev-east
AWS_CLUSTER_WEST=eks-cluster-dev-west

# =============================================================================
# Azure Configuration
# =============================================================================
# Terraform State
AZURE_STORAGE_ACCOUNT_NAME=yourstorageaccount
AZURE_CONTAINER_NAME=tfstate
AZURE_STATE_RESOURCE_GROUP=terraform-state-rg

# ACR Configuration (populated after registries/azure deployment)
AZURE_ACR_NAME=youracrname
AZURE_ACR_REGISTRY_ID=/subscriptions/SUB_ID/resourceGroups/acr-registry-rg/providers/Microsoft.ContainerRegistry/registries/youracrname

# AKS Cluster Names
AZURE_CLUSTER_EAST=aks-cluster-dev-east
AZURE_CLUSTER_WEST=aks-cluster-dev-west
AZURE_RG_EAST=aks-multiregion-demo-dev-east-rg
AZURE_RG_WEST=aks-multiregion-demo-dev-west-rg

# =============================================================================
# Kubernetes Application
# =============================================================================
K8S_DEPLOYMENT_NAME=basic-demo-microservice-01
K8S_NAMESPACE=default
K8S_REPLICAS=3

# =============================================================================
# DNS Configuration (for traffic validation)
# =============================================================================
DOMAIN_NAME=armandoherra.games
SUBDOMAIN=multi-cloud
AWS_POOL_SUBDOMAIN=aws-pool
